{

    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Test",
            "type": "shell",
            "command": ". ${workspaceFolder}/scripts/build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}